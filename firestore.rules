rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isValidId(id) {
      return id is string && id.size() > 0 && id.size() <= 128;
    }

    // Default deny catch-all
    match /{document=**} {
      allow read, write: if false;
    }

    // Health check ping
    match /_connection_test/{docId} {
      allow read: if true;
      allow write: if false;
    }

    // Commercial Client Accounts
    match /accounts/{accountId} {
      allow get: if isValidId(accountId);
      allow list: if true;
      allow write: if isValidId(accountId);
    }

    // Screen Devices (TVs, displays)
    match /screens/{screenId} {
      allow get: if isValidId(screenId);
      allow list: if true;
      allow write: if isValidId(screenId);
    }

    // Display Schedules & Playlists
    match /schedules/{scheduleId} {
      allow get: if isValidId(scheduleId);
      allow list: if true;
      allow write: if isValidId(scheduleId);
    }

    // Media Assets & Videos
    match /media/{mediaId} {
      allow get: if isValidId(mediaId);
      allow list: if true;
      allow write: if isValidId(mediaId);
    }

    // Inquiries, Contact Requests & Subscription Interests
    match /inquiries/{inquiryId} {
      allow get: if isValidId(inquiryId);
      allow list: if true;
      allow write: if isValidId(inquiryId);
    }
  }
}
